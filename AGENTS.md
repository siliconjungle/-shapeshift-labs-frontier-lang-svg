# Frontier Lang SVG Adapter Notes

Keep this package focused on runtime-neutral SVG source evidence: element identity, local references, paint/runtime boundaries, and fail-closed proof gaps.

Project-level safe merge, JSX embedding, HTML embedding, CSS cascade admission, and browser/runtime proof routing belong above this package in `@shapeshift-labs/frontier-lang-compiler` and runtime proof packages.

Do not print or commit npm tokens. Release credentials live in `/Users/james/Documents/json-diff/.env`.
